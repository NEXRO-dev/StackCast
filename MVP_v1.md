# StackCast MVP v1 パーソナルニュース仕様書

## 0. 文書情報

| 項目 | 内容 |
| --- | --- |
| プロダクト名 | StackCast |
| バージョン | MVP v1（パーソナルニュース中心の改訂版） |
| 対象 | iOSアプリ、Share Extension、Live Activity、バックエンド、管理画面 |
| 対応言語 | 日本語、英語 |
| 文書の目的 | パーソナルニュースを主機能、URL保存とCast生成をサブ機能とする新しい製品体験を定義する |
| 基準日 | 2026年8月19日 |

## 1. プロダクト概要

StackCastは、ユーザーの興味と利用履歴から自分に合うニュースを提案し、見つけた記事をそのまま読む、保存する、またはAI音声Castへ変換して聴けるiOSアプリである。

初回利用時に年代と興味ジャンルを選ぶと、ホームへパーソナライズされたニュースが表示される。記事を開く、保存する、非表示にする、Castにするなどの行動は「メモリ」へ反映され、使うほど推薦がその人に合うようになる。従来の共有シートやURL入力による記事保存とCast生成は残し、ニュース発見後の消化手段として提供する。

毎日午前1時（日本時間）にバックエンドのバッチ処理が共通ニュース候補を更新し、各ユーザーのジャンルとメモリに合わせて「今日の5件」を確定する。Plus／Proユーザーは任意で、5件をまとめたデイリーニュースCastを1日1本自動生成できる。

### プロダクトステートメント

> あなたに必要なニュースを、見つけて、読んで、聴ける。

### 解決する課題

- 自分に必要なニュースを探す入口が分散している
- 一般的なランキングでは、自分の関心に合わない記事が多い
- 保存した記事を読む時間が取れない
- 移動中や作業中は画面を見続けられない
- 未読記事が増え、何から読むか決めにくい
- 複数の記事を短時間でまとめて把握したい

## 2. 対象ユーザー

### 主対象

ニュースを日常的に確認し、自分の関心に合う情報を効率よく見つけ、必要に応じて音声でも消化したいユーザー。

### 主な利用状況

- 通勤、通学、徒歩、運転など画面を見にくい時間
- 家事、運動、休憩中
- ホームを開いて自分向けの最新ニュースを確認するとき
- 見つけた記事を保存または非表示にし、推薦を調整するとき
- 保存記事をまとめて消化したいとき
- 重要な記事を音声と文字起こしの両方で確認したいとき

### Jobs to be Done

> 自分の関心に合うニュースを探す手間を減らし、気になった記事をその場で読むか保存し、画面を見られない時間にはCastとして聴きたい。

## 3. 改訂MVPの主要体験

```text
アカウント作成・ログイン
  → 年代と興味ジャンルを選択
  → 毎日午前1時に自分向けニュース5件が更新される
  → ホームで「今日の5件」を見る
  → 記事を開く／保存する／興味なしを選ぶ
  → 行動をメモリへ反映して次の推薦を改善
  → Plus／Proは任意で5件をまとめたデイリーCastを自動生成
  → 保存記事または任意URLから3〜4件を選択
  → AIデータ利用へ同意
  → Castを生成
  → 音声を再生／文字起こしを読む
  → 必要に応じてお気に入り・ダウンロード・共有
```

### 成功条件

- ユーザーがメール、Google、Appleのいずれかでログインできる
- 初回に年代と3つ以上の興味ジャンルを選べる
- 毎日午前1時（日本時間）のバッチ処理で、ユーザーごとに異なるニュース5件が確定する
- ホームにユーザー別の「今日の5件」が表示される
- 記事の閲覧、保存、非表示が次回以降の推薦順へ反映される
- Plus／Proユーザーが明示的に有効化した場合、5件をまとめたデイリーCastが1日1本生成される
- ユーザーがメモリの内容を確認、編集、削除できる
- 共有シートまたはURL入力から記事を保存できる
- 本番環境では3〜4件の記事からCastを生成できる
- 生成したCastを再生し、シーク、一時停止、速度変更ができる
- バックグラウンド、ロック画面、Dynamic Islandから再生を操作できる
- Castを文字起こしで確認できる
- Castを端末へ保存し、オフラインで再生できる
- サブスクリプション購入・復元後、DBの実効プランがアプリへ反映される
- ユーザーがアプリ内からアカウントを削除できる

## 4. MVPに含まれる機能

1. 年代と興味ジャンルを含むオンボーディング
2. メール、Google、Appleによる認証
3. 日本語・英語表示
4. GDELT等を利用したニュース候補取得
5. 毎日午前1時（日本時間）の共通ニュース取得バッチ
6. ジャンルとメモリに基づくユーザー別「今日の5件」の選定
7. ホーム上のパーソナルニュースフィード
8. 記事の閲覧、保存、興味なし、推薦理由表示
9. Plus／Pro向けデイリーニュースCastの自動生成
10. アカウント内のメモリ確認・編集・削除
11. 年代別の匿名集計トレンド
12. URL手動入力とShare Extensionによる記事保存
13. 記事タイトルとサイト情報の取得
14. 未読、期限間近、完了のストック管理
15. 複数記事の選択とAI Cast生成
16. AI処理前の明示的な同意
17. Cast一覧、検索、並び替え、カテゴリ絞り込み
18. Cast再生、一時停止、±10秒、シーク、速度変更
19. 再生位置の保存と文字起こし
20. お気に入り、音声ダウンロード、オフライン再生
21. ミニプレイヤー、バックグラウンド再生
22. ロック画面、Live Activity、Dynamic Island
23. 公開Castリンクの発行、共有停止、通報
24. Free、Plus、Proのサブスクリプション
25. RevenueCat購入情報とバックエンドDBの同期
26. 管理画面からのプラン上書き
27. アカウント内のログ、設定、サポート、法務導線
28. アプリ内アカウント削除

## 5. MVPに含めない機能

- Android版
- Web版のアプリ本体
- X、Redditなど外部サービスのアカウント連携
- ユーザー同士のフォロー、コメント、DM、投稿
- 個人が特定できる年代別ランキング
- 共同プレイリストや複数人での共同編集
- 記事のタグ、フォルダ、コメント
- 複数端末間でのストック記事の完全同期
- 音声話者や声質のユーザーカスタマイズ
- 広告配信、IDFA、広告トラッキング

## 6. 認証とアカウント

### 6.1 認証方法

- メールアドレスによる新規登録とログイン
- メール確認コードを使った登録
- Google Sign-In
- Sign in with Apple
- セッショントークンはKeychainへ保存する

### 6.2 アカウント画面

- プロフィール画像、表示名、メールアドレス
- 現在の実効プラン
- 記事の完了件数と未消化件数を確認できるログ
- 推薦に使われている興味ジャンルとメモリ
- メモリの編集、個別削除、全削除
- 設定画面への導線
- ログアウト
- アカウント削除
- タブバーにはログイン中ユーザーの丸型プロフィール画像を表示する

### 6.3 アカウント削除

ユーザーが削除を確定すると、バックエンド上の認証セッション、認証ID、プロフィール、課金同期情報、ユーザー本体を削除する。ユーザーに関連するR2音声オブジェクトも削除対象とする。

### 6.4 初回パーソナライズ

- 年代は正確な生年月日ではなく範囲で選択する
- 性別は任意回答とし、「回答しない」を常に用意する
- 興味ジャンルを最低3つ選ぶ
- 選択例：テクノロジー、ビジネス、科学、エンタメ、スポーツ、社会、国際、健康、文化
- 年代や性別を未回答でも、ジャンルだけで主要機能を利用できる
- 後からアカウント画面で変更できる

### 6.5 メモリ

メモリは、推薦を個人化するためのユーザー別プロフィールであり、SNS上の公開プロフィールではない。

保存する信号：

- 明示的に選んだ興味ジャンル
- 記事の表示、閲覧、滞在、保存、完了
- 「興味なし」とした記事・トピック・媒体
- Castへ追加した記事と再生完了率
- ユーザーが手動で追加した好み・避けたい話題

原則：

- センシティブな属性や思想を閲覧履歴から勝手に推定しない
- 推薦理由を「テクノロジーを選択しているため」など短く説明する
- ユーザーがメモリを確認、修正、削除できる
- メモリを消しても、アカウントや購入状態は維持できる
- アカウント削除時はメモリと推薦イベントも削除する

## 6A. パーソナルニュース推薦

### 6A.1 候補取得

- バックエンドがGDELT DOC 2.0等から最新記事候補を取得する
- GDELTは候補検索として使い、最終順位はStackCast側で決める
- 日本語ユーザー向けには英語キーワードへの展開、言語・国・媒体フィルターを行う
- 重複URL、同一内容、古すぎる記事、取得不能記事を除外する
- GDELT障害時に備え、キャッシュと代替ニュースソースを追加できる構造にする

### 6A.2 ランキング

候補記事は以下を組み合わせて順位付けする。

1. 初回選択ジャンルとの一致
2. メモリ上の興味トピックとの一致
3. 記事の新しさ
4. 情報源の品質と重複の少なさ
5. 閲覧・保存・Cast化など過去行動との類似性
6. 同じ話題や媒体が続きすぎない多様性
7. 興味なし・除外設定との不一致

### 6A.3 ホーム上の操作

- 記事を開く
- ストックへ保存する
- あとでCastに追加する
- 興味なしにする
- 同じ話題を減らす
- 同じ媒体を減らす
- 推薦理由を確認する
- フィードを更新する

記事を開いた事実だけで強い好みと判断せず、滞在、保存、Cast化、非表示など複数の信号を組み合わせる。

### 6A.4 年代別トレンド

- 「同じ年代でよく読まれている記事」を匿名の集計情報として表示できる
- 個人のプロフィール、名前、閲覧履歴、フォロー関係は表示しない
- 集計人数が少ない区分は表示しない
- SNS化せず、ニュース発見を補助するランキングに限定する

### 6A.5 今日の5件と日次バッチ

- 毎日午前1時（`Asia/Tokyo`）にバックエンドがGDELT等から全ユーザー共通の記事候補を取得する
- GDELTへの問い合わせはユーザー単位で行わず、ジャンル別の共通記事プールを1回更新する
- 共通記事プールから、各ユーザーの明示ジャンル、メモリ、既読、非表示、媒体・話題の多様性を使って5件を選ぶ
- 5件の構成は、強い興味に合う3件、関連ジャンル1件、発見枠1件を初期値とする
- `(user_id, edition_date)` を一意にし、同じ日の重複生成を防ぐ
- バッチ失敗時は既存の共通キャッシュから選定し、それも失敗した場合は前日の5件を表示する
- ホームのPull to RefreshではGDELTを呼ばず、その日の確定済み5件をDBから再取得する

### 6A.6 デイリーニュースCast

- 「今日の5件」を記事別の5本ではなく、5件をまとめた1本のCastとして生成する
- 自動生成の対象は、Plus／Pro、AI処理への同意済み、自動生成ON、直近7日以内に利用、有効な生成枠あり、の全条件を満たすユーザーとする
- Freeユーザーには5件のニュースカードを提供し、Cast化は既存の手動生成導線を使う
- 記事全文を読み上げず、各記事の短い要約、出典、元記事リンク、記事間のつなぎを含む台本を作る
- 記事単位の要約は共有キャッシュし、同じ記事をユーザーごとに再要約しない
- 生成ジョブはキューで順番に処理し、OpenAIとFish Audioへの同時リクエスト数を制御する
- 失敗した工程だけを再試行し、ニュース5件の表示はCast生成の成否に依存させない
- 同じ記事集合・言語・音声設定の完成音声は、権利・プライバシー上問題がない範囲で再利用できる設計にする

## 7. 記事ストック

### 7.1 保存方法

- アプリ内でURLを入力する
- iOS共有シートからStackCastを選択する
- `http` または `https` のURLだけを受け付ける
- 同一URLを再保存した場合は重複を残さず、最新の保存として扱う
- Share Extensionはアプリ本体を開かなくてもApp Groupへ保存できる

### 7.2 記事状態

| 状態 | 定義 |
| --- | --- |
| 未読 | 保存済みで、元記事の閲覧やCast化が完了していない |
| 進行中 | 元記事をアプリ内ブラウザで開いた |
| 完了 | ユーザーが完了へ変更した、またはCast生成に使用された |

### 7.3 ストック画面

- 「未読」「期限間近」「完了」で切り替える
- 記事タイトル、媒体、保存日、期限情報を表示する
- 元記事をアプリ内ブラウザで開ける
- 記事を完了または削除できる
- 未完了記事を選択し、Cast生成へ進める
- 本番環境のCast生成は3〜4件の記事を必要とする

### 7.4 保存期間と件数

| プラン | 未完了記事の上限 | 保存期間 |
| --- | ---: | --- |
| Free | 10件 | 5日 |
| Plus | 実装上は端末内件数上限なし | 15日 |
| Pro | 実装上は端末内件数上限なし | 無期限 |

期限を過ぎた未完了記事は端末内の有効なストックから除外する。完了記事は期限にかかわらずログ用に保持する。

## 8. Cast生成

### 8.1 入力

- 本番環境では3〜4件の記事を選択する
- 開発環境では1〜4件を許可する
- 本番で選択できるCast時間は5分、10分、15分、20分
- FreeプランのアプリUIは10分だけを許可する
- 有料プランは5分、10分、15分、20分およびカスタム時間の選択導線を持つ
- デイリーニュースCastは確定済みの「今日の5件」を入力とし、記事の手動選択を要求しない
- PlusのデイリーCastは初期値5分、Proは設定可能な範囲で長さを変更できる構成を検討する

### 8.2 AI処理

1. バックエンドが記事URLから本文を取得する
2. OpenAIのModerationで入力内容を確認する
3. OpenAIが記事の要約、導入、記事間のつなぎ、締めを含む台本を生成する
4. Fish Audioが台本から音声を生成する
5. 音声ファイルをCloudflare R2へ保存する
6. Castの要約、文字起こし、再生時間、音声参照先をTursoへ保存する

### 8.3 AI利用への同意

初回のCast生成前に、選択した記事のURL・本文がOpenAIとFish Audioへ送信されること、要約・音声生成が目的であること、記事内に個人情報が含まれる可能性があることを説明し、「同意して続ける」を要求する。

### 8.4 生成結果

- 成功したCastはポッドキャスト画面へ表示する
- 使用したストック記事は完了へ変更する
- 失敗時は再試行、選択記事の削除、サポート問い合わせを選べる
- 不適切コンテンツ、クレジット不足、入力件数不正では無条件の再試行を案内しない

### 8.5 クレジット

- 5分単位を1クレジットとして計算する
- 10分Castは2クレジットを消費する
- 現行バックエンドの月間クレジット上限はFree 3、Plus 40、Pro 100である
- Lifetimeは合計150クレジットとして扱う実装がある
- デイリーニュースCastも生成クレジットを消費し、予約・成功時消費・失敗時解放を既存台帳で管理する
- 現行のPlus月40クレジットでは5分Castを毎日1本生成できるが、手動生成分も含むため上限到達時はその日の自動生成をスキップする

## 9. ポッドキャスト画面

### 9.1 Cast一覧

- 完成済みで音声を持つCastを表示する
- 「すべて」「未再生」「お気に入り」「ダウンロード済み」で絞り込む
- タイトル検索
- 新しい順／古い順の並び替え
- お気に入りの追加・解除
- 音声のダウンロード・削除
- 公開リンクの作成

### 9.2 Cast詳細

- Cast画像とタイトル
- 経過時間、総時間、シークバー
- 再生、一時停止、10秒戻る、10秒進む
- 再生速度：0.75x、1.0x、1.25x、1.5x、2.0x
- 文字起こしの全画面表示
- 公開Castの共有、共有停止、通報
- 縦向きと横向きの専用レイアウト

### 9.3 再生状態

- Castごとの再生位置を端末へ保存する
- アプリ再起動後も保存位置から再開できる
- 最後まで再生したCastは再生済みとして扱う
- アプリ内の詳細画面、ミニプレイヤー、Dynamic Island、ロック画面で同じ再生位置を参照する

## 10. バックグラウンド・システム連携

### 10.1 バックグラウンド再生

- Freeプランではアプリがバックグラウンドへ移ると再生を停止する
- Plus、Pro、Lifetimeではバックグラウンド再生を継続できる

### 10.2 ロック画面

`MPNowPlayingInfoCenter` と `MPRemoteCommandCenter` を使用し、Castタイトル、再生時間、再生状態を表示する。再生、一時停止、±10秒、シークをアプリを開かず操作できる。

### 10.3 Live ActivityとDynamic Island

- 有料プランの再生時にLive Activityを開始する
- コンパクト表示ではCast画像と再生時間を表示する
- 展開表示では画像、タイトル、進捗、再生／一時停止、±10秒を表示する
- App Intentにより、アプリを前面へ開かず操作する
- 再生終了時にLive Activityを終了する

### 10.4 オフライン再生

- Cast音声をアプリのDocuments配下へ保存する
- ダウンロード済みCastの索引を端末へ保持する
- ネットワークがなくても保存済み音声を再生できる
- ユーザーはダウンロードを削除できる

## 11. 公開Cast共有

- Cast所有者だけがランダムな共有URLを発行できる
- 共有URLは `https://stackcast.app/c/{token}` 形式とする
- URLを受け取ったユーザーはアプリまたはWebページから公開Castへアクセスできる
- 所有者は共有を停止できる
- 閲覧者は不適切コンテンツ、著作権、プライバシーを理由に通報できる
- 公開URLは元記事全文の無断転載ではなく、AI生成Castを共有する

## 12. ホームとログ

### 12.1 ホーム

- ホームの主役をパーソナルニュースフィードとする
- 上部に「今日の5件」を表示する
- 5件は毎日午前1時（日本時間）に更新し、ユーザーごとに内容を変える
- 記事カードには画像、タイトル、媒体、公開時刻、ジャンル、短い推薦理由を表示する
- 記事を開く、保存、Cast候補へ追加、興味なしを選べる
- 「同じ年代で読まれている」「興味ジャンルの新着」などの補助セクションを置ける
- 未消化記事数、期限が近い記事、今週の消化数はフィード下部または別セクションへ移す
- Cast時間選択と保存記事からのCast生成はサブ導線として残す
- Plus／ProでデイリーCastが完成している場合は、5件の近くに再生導線を表示する

### 12.2 ログ

- 完了記事数
- 未消化記事数
- 完了した記事の履歴
- ログ画面は独立タブではなくアカウント画面へ統合されている
- ログは本人だけが閲覧でき、他ユーザーへ公開しない

## 13. 設定

- 表示言語：日本語／英語
- 外観：システム／ライト／ダーク
- 既定のCast時間
- 既定の再生速度
- 期限通知の設定表示
- 記事とAI処理について
- サポート問い合わせ
- 利用規約
- プライバシーポリシー
- アプリバージョン
- 下方向へのPull to Refreshによる実効プラン更新
- 興味ジャンルと推薦メモリの管理
- パーソナライズ利用の有効・無効
- デイリーニュースCast自動生成の有効・無効（Plus／Pro）

## 14. 課金と実効プラン

### 14.1 プラン

| 機能 | Free | Plus | Pro |
| --- | --- | --- | --- |
| 記事ストック | 10件、5日 | 15日 | 無期限 |
| Cast時間 | 10分 | 5・10・15・20分 | 全時間とカスタム導線 |
| バックグラウンド再生 | 不可 | 可 | 可 |
| Live Activity | 不可 | 可 | 可 |
| オフライン再生 | 画面上の制限要整合 | 可 | 可 |
| Castクレジット | 月3 | 月40 | 月100 |
| 今日のニュース5件 | 可 | 可 | 可 |
| デイリーCast自動生成 | 不可 | 1日1本・5分を初期値 | 1日1本・長さ設定対応 |

月額・年額の商品をRevenueCat Offeringから読み込む。具体的な販売価格はApp Store Connectの商品情報を正とし、アプリへハードコードしない。

### 14.2 購入状態の正

- ストア購入状態の取得とレシート管理はRevenueCatを使用する
- RevenueCat Webhookで購入・更新・キャンセル・期限切れをバックエンドへ同期する
- アプリ内で利用する実効プランはバックエンドDBの `effectivePlanTier` を正とする
- アプリとバックエンドは同じ実効プランを参照する
- RevenueCat応答に失敗しても、DBから実効プランを取得できる場合はアクセス権を維持する
- 期限切れになった課金行はDB上で非アクティブへ正規化する

### 14.3 管理者上書き

- 管理画面からユーザーのプランをFree、Plus、Pro等へ一時的または無期限に上書きできる
- 有効な管理者上書きはRevenueCatの購入状態より優先して実効プランとなる
- 上書き期限切れまたは無効化後はFreeへ戻る

## 15. エラーと例外

| 状況 | ユーザーへの対応 |
| --- | --- |
| URLが不正 | 入力内容を確認するよう案内する |
| Freeの記事上限 | アップグレード導線を表示する |
| 本文取得失敗 | 別の記事を選ぶよう案内する |
| Cast対象件数が不正 | 本番では3〜4件を選ぶよう案内する |
| AI Moderationで拒否 | 対象記事では生成できないことを案内する |
| OpenAI生成失敗 | 後で再試行するよう案内する |
| Fish Audio生成失敗 | 後で再試行するよう案内する |
| クレジット不足 | 上限到達として再試行を無効にする |
| Cast一覧取得失敗 | ダウンロード済みCastは端末から引き続き表示する |
| オフライン | 画面上部にオフライン表示を出す |
| ニュース候補取得失敗 | 保存記事とキャッシュ済み候補を表示し、再読み込みを案内する |
| 推薦候補がない | ジャンル変更と新着ニュースの汎用フィードを案内する |
| 課金取得失敗 | 有効プランを即時に誤解除せず、DB状態を再確認する |
| 共有Cast取得失敗 | 共通エラーアラートを表示する |
| アカウント削除失敗 | 削除されていないことを明示して再試行可能にする |

## 16. データ要件

### 16.1 端末・App Group

- 保存記事URL、タイトル、媒体、保存日時、状態、完了日時
- お気に入りCast ID
- ダウンロード済みCastと音声ファイル
- Cast再生位置
- 表示言語、外観、再生速度、AI同意状態
- 端末内だけで保持する場合の一時的な閲覧イベントと推薦キャッシュ
- 認証セッショントークンはKeychainへ保存する

### 16.2 バックエンドDB

- ユーザー、プロフィール、認証ID、セッション
- Cast、Castソース、要約、文字起こし、生成状態、音声オブジェクトキー
- Castクレジット期間、予約、消費台帳
- 公開Castトークン、共有状態、通報
- RevenueCat課金同期情報
- 管理者プラン上書き
- 年代範囲、任意の性別、選択ジャンル
- 推薦メモリ、閲覧・保存・非表示・Cast化イベント
- 記事候補キャッシュ、推薦スコア、推薦理由
- ユーザー別の日次ニュース版、選定された5件、自動Cast生成状態
- デイリーCast生成ジョブ、試行回数、失敗理由、次回再試行時刻
- 年代別トレンド用の匿名集計

### 16.3 外部サービス

| サービス | 用途 |
| --- | --- |
| GDELT DOC 2.0 | 最新ニュース候補の検索・取得 |
| OpenAI | 記事内容の安全性確認、要約、Cast台本生成 |
| Fish Audio | Cast音声生成 |
| Cloudflare R2 | 生成音声の保存 |
| RevenueCat | App Store課金・購入状態管理 |
| Turso | バックエンドDB |

## 17. 非機能要件

- API秘密鍵をiOSアプリへ含めない
- GDELT等のニュース取得はバックエンド経由とし、取得元変更をiOS更新なしで行えるようにする
- 推薦ランキングは取得元と分離し、GDELTを単一障害点にしない
- 推薦イベントは必要最小限を保存し、保持期間を定義する
- 年代・性別・ジャンル・閲覧履歴の利用目的をプライバシーポリシーとApp Privacyへ反映する
- パーソナライズを無効にしたユーザーには汎用フィードを提供する
- 年代別トレンドには最小集計人数を設け、個人を推定できないようにする
- Cast生成はバックエンドで実行する
- 日次処理は`Asia/Tokyo`の午前1時を基準とし、再実行しても同じ日の版やCastを重複生成しない
- GDELT取得、ユーザー別5件選定、AI台本、音声生成を分離し、キューで外部API負荷を制御する
- セッションは期限付きとし、無効なセッションには401を返す
- RevenueCat WebhookはAuthorizationヘッダーを検証する
- 公開Castは推測困難なトークンを使用する
- 同一Webhookの重複・順序逆転に耐えられるようイベント時刻を比較する
- Cast生成クレジットは予約・消費・解放を台帳で管理する
- オフライン時も保存記事とダウンロード済みCastを閲覧・再生できる
- Dynamic Type、VoiceOverラベル、Reduce Motionを考慮する
- iPhoneの縦向き・横向きへ対応する
- リリース構成では本番APIとApp Store用RevenueCatキーを使用する

## 18. 現時点で残る仕様・実装の要整合

以下は現行コードで確認できた不一致または未接続項目であり、完成済みMVP要件として扱わない。

1. パーソナルニュース候補取得、推薦ランキング、メモリAPI・DBは新規実装が必要である。
2. 初回の年代・ジャンル選択と、アカウント内のメモリ管理UIは新規実装が必要である。
3. 現在のホーム画面は推薦フィードになっておらず、全面的な再構成が必要である。
4. サブスクリプション画面のPlus説明は「月200本」と表示するが、バックエンドはPlus月40クレジットであり、10分Cast換算では月20本となる。
5. Free画面は「月3本」と表示するが、バックエンドは月3クレジットであり、10分Castは2クレジットを消費する。
6. Pro画面の保存件数とCast時間の具体的上限が表示文言だけでは確定していない。
7. オフラインダウンロード操作は画面上でプラン制限が統一されていない。
8. 設定画面の通知トグルは表示されるが、ローカル通知の登録処理は未実装である。
9. 設定画面の既定Cast時間は保存・生成フローとの接続が未完了である。
10. アカウント画面の一部メニューは確認用の仮メニューで、遷移先が未接続である。
11. ストック画面の選択UIは5件以上を選択できるが、バックエンドは最大4件として拒否する。
12. `Config.revenueCatEntitlementID` に旧名称 `StashCast Pro` が残っており、RevenueCat設定との一致確認が必要である。
13. カスタムCast時間UIは1〜60分を選択できるが、本番バックエンドが受け付ける時間は5・10・15・20分だけである。
14. デイリーニュースCastを導入する前に、現行クレジット上限と手動Castを併用した場合の月間上限表示を確定する必要がある。

## 19. MVP受け入れ条件

### 認証

- メール、Google、Appleで登録・ログインできる
- 再起動後にセッションを復元できる
- ログアウトとアカウント削除を完了できる

### 記事

- 初回に選択したジャンルからホームのニュースフィードを生成できる
- 午前1時の日次バッチでユーザー別の5件が生成され、同じユーザー・同じ日には重複しない
- 異なるジャンル・メモリを持つユーザーでは5件の内容または順位が変わる
- 記事の閲覧、保存、興味なしが推薦メモリへ記録される
- 行動後のフィード更新で順位または候補が変化する
- 各記事の推薦理由を確認できる
- メモリを編集・全削除でき、削除後は初期ジャンル中心の推薦へ戻る
- パーソナライズを無効にしても新着ニュースを閲覧できる
- URL入力とShare Extensionの両方から記事を保存できる
- 未読、期限間近、完了を切り替えられる
- Freeの10件上限と保存期間が反映される
- 期限切れ記事が有効な未読一覧から除外される

### Cast生成

- 本番環境で3〜4件の記事を選び、AI同意後にCastを生成できる
- 成功時に要約、文字起こし、音声が保存される
- 生成失敗時に理由に応じた次の操作を選べる
- クレジットが重複消費されない
- 条件を満たすPlus／Proユーザーには、5件をまとめたデイリーCastが1日1本だけ生成される
- 自動生成OFF、休眠、同意未完了、上限到達のユーザーには自動Castを生成しない
- Cast生成が失敗しても、その日のニュース5件は閲覧できる

### 再生

- 再生、一時停止、±10秒、シーク、速度変更ができる
- アプリ内の各再生UIで時間が一致する
- 再起動後に途中位置から再開できる
- 有料プランでバックグラウンド、ロック画面、Dynamic Island操作ができる
- ダウンロード済みCastを機内モードで再生できる

### 課金

- TestFlightで購入、更新、キャンセル、期限切れ、復元を確認できる
- RevenueCat Webhook後にDBの実効プランが更新される
- アプリの表示とバックエンドの権限制御が同じ `effectivePlanTier` を使う
- 管理者上書きがアプリへ反映され、期限後に解除される

### 共有と法務

- 所有者が公開リンクを発行・停止できる
- 閲覧者が公開Castを通報できる
- 利用規約、プライバシーポリシー、AI処理説明へアプリ内から移動できる
- アカウント削除時にDBデータとR2音声を削除できる
- アカウント削除時に推薦メモリと閲覧イベントも削除できる
- 年代別トレンドから個人を特定できない

## 20. リリース判定

以下を満たした場合、改訂MVP v1を本番提供可能と判断する。

- 初回ジャンル選択 → ニュース推薦 → 閲覧／保存 → 推薦更新の主要フローが実機で完走する
- 午前1時の日次バッチ → ユーザー別5件 → 対象ユーザーのデイリーCast生成がテスト環境で完走する
- 保存 → 選択 → AI同意 → Cast生成 → 再生の主要フローが実機で完走する
- GDELT障害時もアプリ全体が利用不能にならず、キャッシュまたは汎用フィードを表示する
- メモリの確認、編集、全削除、パーソナライズ無効化を確認する
- App Privacyとプライバシーポリシーへ年代、ジャンル、閲覧履歴、Product Personalizationを反映する
- iPhone実機とTestFlightでShare Extension、バックグラウンド、ロック画面、Dynamic Islandを確認する
- Free、Plus、Proの表示上限とバックエンド上限を一致させる
- 購入、復元、期限切れ、管理者上書きを実データで確認する
- オフライン、低速通信、AI失敗、課金取得失敗を再現して操作を継続できる
- アカウント削除後にDBとR2から対象データが削除される
- App Store Connectのアプリ情報、サブスクリプション、プライバシー申告が実装と一致する
- Release Archiveで本番URL、Bundle ID、Entitlements、RevenueCatキーを確認する
- 利用規約、プライバシーポリシー、サポートURLが公開されている
